# Thread Algo Search Lab Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | input width | 134 | watch |
| stress | search depth | 141 | ship |
| edge | boundary pressure | 133 | watch |
| recovery | complexity | 241 | ship |
| stale | input width | 138 | watch |

Start with `recovery` and `edge`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `complexity` against `boundary pressure`, not the raw score alone.
